var a = 3
을 a := 3
으로 쓸 수 있다.
단, :=(Short Assignment Statement)는 펑션 내부에서만 쓸 수 있으며
외부에서는 지역변수와 전역변수 모두 사용할 수 잇다.

:=는 선언과 동시에 할당한다.
즉, a := 3 을했다면
    a := 5 는 불가능하다. 이미 a가 있기에 선언이 안되기 때문이다. 값을 변경할려면
    a = 5 라 해야한다.
